{{ form_start(form) }}
{{ form_errors(form) }}
<br>
{{ form_row(form.type) }}
<br>
{{ form_row(form.service) }}
<br>
<button class="btn admin-button">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}