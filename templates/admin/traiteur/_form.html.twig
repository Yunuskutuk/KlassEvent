{{ form_start(form) }}
{{ form_errors(form) }}
{{ form_widget(form) }}
<a type="button" class="btn btn-dark">{{ button_label|default('Sauvegarder') }}</a>
{{ form_end(form) }}
<a type="button" class="btn btn-dark" href="{{ path('admin_traiteur_index') }}">Retour</a>
