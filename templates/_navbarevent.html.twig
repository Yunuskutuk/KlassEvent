<div class="py-1 bg-black top {{ app.session.flashbag.peek('success')|length > 0 ? '' : '' }}">
	<div class="container">
		<div class="row no-gutters d-flex align-items-start align-items-center px-md-0">
			<div class="col-lg-12 d-block">
				<div class="row d-flex">
					<div class="col-md-2 pr-4 d-flex topper align-items-start">
						<div class="icon mr-2 d-flex justify-content-center align-items-center">
							<a href="/trk/event">
								<img class="nav-flag" src="{{asset('build/images/Flag_of_Turkey.png')}}" width="36"></a>
							<a href="/event">
								<img class="nav-flag" src="{{asset('build/images/Flag_of_France.png')}}" width="36"></a>
						</div>
					</div>
					<div class="col-md-2 pr-4 d-flex topper align-items-center">
						<div class="icon mr-2 d-flex justify-content-center align-items-center">
							<span class="icon-phone2"></span>
						</div>
						<span class="text">{{ 'Klass.phone'|trans }}{# 07 80 55 50 17 #}</span>
					</div>
					<div class="col-md-2 pr-4 d-flex topper align-items-center">
						<div class="icon mr-2 d-flex justify-content-center align-items-center">
							<span class="icon-paper-plane"></span>
						</div>
						<span class="text">{{ 'Klass.courriel'|trans }}</span>
					</div>
					<div class="col-md-5 pr-4 d-flex topper align-items-center text-lg-right justify-content-end">
						<p class="mb-0 register-link">
							<span>{{ 'event.index.perimetre'|trans }}{# Déplacements jusqu'à 350km autour de Strasbourg #}</span>
						</p>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>
<nav class="navbar navbar-expand-lg navbar-dark ftco_navbar bg-dark ftco-navbar-light" id="ftco-navbar">
	<div class="container align-items-center">
		<a class="navbar-brand" href="{{ path('event_index') }}">Klass Event</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#ftco-nav" aria-controls="ftco-nav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="oi oi-menu"></span>
			{{ 'Menu'|trans }}
			{# Menu #}
		</button>
		<div class="collapse navbar-collapse" id="ftco-nav">
			<div class="navbar-nav ml-auto">
				<li class="flag align-items-start">
					<div class="icon mr-2 d-flex  align-items-center contact-g flag">
						<a href="/trk/event">
							<img class="nav-flag" src="{{asset('build/images/Flag_of_Turkey.png')}}" width="36"></a>
						<a href="/event">
							<img class="nav-flag " src="{{asset('build/images/Flag_of_France.png')}}" width="36"></a>
					</div>
				</li>
				<li class="nav-item">
					<a href="{{ path('home') }}" class="nav-link">{{ 'accueil'|trans }}{# Accueil #}</a>
				</li>

				{{ render(controller('App\\Controller\\NavbarEventController::quoteNavbar')) }}
				{# menu déroulant Devis #}

				{{ render(controller('App\\Controller\\NavbarEventController::gallery')) }}
				{# menu déroulant gallery #}

				<div id="nav-options" class="btn-group pull-right hidden-xs">
					<button type="button" class=" btn-dropdown dropdown-toggle thumbnail" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
						<span class="glyphicon glyphicon-education" aria-hidden="true"></span>
						{{ 'location'|trans }}
						{# Location #}
					</button>
					<ul class="dropdown-menu">
						<li>
							<a href="{{ path('rent_decoration') }}">
								<p class="text-center">{{ 'rent.deco'|trans }}{# Décoration de table #}</p>
							</a>
						</li>
						<li>
							<a href="{{ path('rent_dishes') }}">
								<p class="text-center">{{ 'rent.Vaisselle'|trans }}{# La vaisselle #}</p>
							</a>
						</li>
						<li>
							<a href="{{ path('rent_chair') }}">
								<p class="text-center">{{ 'rent.deco2'|trans }}{# Décoration de chaise #}</p>
							</a>
						</li>
					</ul>
				</div>

				{% if app.user %}
					<li class="nav-item">
						<a href="{{ path('account_show') }}" class="nav-link">{{ 'event.account.compte'|trans }}{# Mon compte #}</a>
					</li>
					<li class="nav-item">
						<a href="{{ path('security_logout') }}" class="nav-link">{{ 'account.logout'|trans }}{# Logout #}</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a href="{{ path('security_login') }}" class="nav-link">{{ 'account.login'|trans }}{# Login #}</a>
					</li>
				{% endif %}
				{% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item">
						<a href="{{ path('admin_index') }}" class="nav-link">Admin</a>
					</li>
				{% endif %}
			</li>
			<li class="nav-item cta contact-g">
				<a href="{{ path('event_contact') }}" class="nav-link">{{ 'contact'|trans }}{# Contact #}</a>
			</li>
			<li class="nav-item contact-p">
				<a href="{{ path('event_contact') }}" class="nav-link">{{ 'contact'|trans }}{# Contact #}</a>
			</li>
		</div>
	</div>
</div></nav>
